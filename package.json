{
  "name": "syncscript-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Workspace automation for the SyncScript extension and signaling server",
  "workspaces": [
    "extension",
    "signaling"
  ],
  "scripts": {
    "lint": "eslint extension/src/**/*.ts signaling/src/**/*.ts",
    "typecheck": "npm run typecheck --workspace extension && npm run typecheck --workspace signaling",
    "build": "npm run build --workspace signaling && npm run compile --workspace extension && npm run tailwind:build --workspace extension",
    "package:vsix": "npm run package:vsix --workspace extension"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.0",
    "eslint": "^9.25.0",
    "globals": "^16.0.0",
    "typescript-eslint": "^8.30.1"
  }
}
